// Kept for local editors that look for this file, but Next/postcss
// will actually use the CommonJS config in postcss.config.cjs.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
